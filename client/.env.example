# ===== REQUIRED ENVIRONMENT VARIABLES =====

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Clerk Authentication (Publishable Key)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key_here

# ===== NOTES =====
# - NEXT_PUBLIC_* variables are embedded in the client bundle at build time
# - These should be set as build arguments when building Docker image
# - For local development, copy this file to .env.local and fill in values
